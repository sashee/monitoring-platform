[package]
name = "mp-host"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
description = "Host primitives shared by the monitoring platform receiver and the clock-correcting collector"

[dependencies]
anyhow = { workspace = true }
libc = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
